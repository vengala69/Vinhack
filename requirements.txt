fastapi>=0.115
uvicorn[standard]>=0.30
pydantic[email]>=2.7

# The wellbeing chat talks to an OpenAI-compatible endpoint over urllib,
# so it needs no vendor SDK. Configure it in .env (see .env.example).
