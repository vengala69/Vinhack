fastapi>=0.115
uvicorn[standard]>=0.30
pydantic[email]>=2.7

# Optional: only needed for the wellbeing chat on the Therapist screen.
# Without it (or without ANTHROPIC_API_KEY) that page falls back to
# scripted replies and everything else still works.
anthropic>=0.40
